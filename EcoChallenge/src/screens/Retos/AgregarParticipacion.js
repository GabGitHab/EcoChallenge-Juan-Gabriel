import { View, Text } from 'react-native'
import React from 'react'

const AgregarParticipacion = ({ route, navigation }) => {
    const { idReto } = route.params;

    return (
    <View>
      <Text>AgregarParticipacion</Text>
    </View>
  )
}

export default AgregarParticipacion